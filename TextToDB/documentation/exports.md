# Exports

The native format of texts and markup is a collection of database objects. A versatile range of export formats can be programatically produced from this:

XML versions of every markup type